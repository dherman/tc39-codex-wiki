#!/bin/bash

cd $(dirname $0)

case "$1" in
  start)
    forever start tc39 webhook $HOME/tc39-ghpages 8081
    ;;
  stop)
    forever stop tc39
    ;;
  status)
    forever list | fgrep tc39
    ;;
  *)
    echo "usage: tc39-webhook start|stop|status"
    exit 1
    ;;
esac
